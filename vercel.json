{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "cd code && corepack pnpm install --frozen-lockfile",
  "buildCommand": "cd code && corepack pnpm build",
  "outputDirectory": "code/.next",
  "devCommand": "cd code && corepack pnpm dev -- --hostname 0.0.0.0 --port 3000"
}
