{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "corepack pnpm install --frozen-lockfile",
  "buildCommand": "corepack pnpm --dir code build",
  "devCommand": "corepack pnpm --dir code dev -- --hostname 0.0.0.0 --port 3000"
}
