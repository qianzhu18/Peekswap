完全理解错了逻辑，是不要对于上传的A图和B图进行裁切，也不要进行遮盖，只是要把这两个图片合并为一个长图，保证实现微信图片预览是A图片，打开因为屏幕居中是B图的效果即可

处理记录：
- 重写合成逻辑，仅通过白色上下留边 + 封面图 + 间隔 + 整张效果图的顺序拼接生成长图，不再对任意图片进行裁切。
- 通过调节上方白底高度（白顶 = 白底 + 间隔 + 整张效果图高度），确保整张长图的中心始终落在封面图区域，对应微信会话预览看到的就是 A 图。
- 预览组件同步使用同一布局数据，移动端/桌面端的预览效果与导出图保持一致，方便快速验证。
- 错误方式复盘：之前把 B 图拆成上下段并尝试在封面区夹入中段，既破坏了原图完整性又出现遮盖；现已彻底移除该逻辑。
