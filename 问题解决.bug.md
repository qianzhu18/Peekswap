现在还有几个初步的问题，
1.就是预览的效果不是微信预览的效果，要保证欢迎微信预览的效果，才可以调整白边的长度
2.即使在手机上面呈现了，但是，在电脑端，依然会暴露，目前这两个点是硬伤

处理记录：
- 通过将封面区高度改为比例控制，并且在预览中按微信会话的居中裁切模拟展示，能够准确调整封面占比。
- 合成长图时按照 B 图的上下分段 + 中部覆盖 A 图的方式生成，桌面端查看也只会在中段看到 A，不再暴露彩蛋。
- Vercel 构建命令改为使用 corepack pnpm，并在项目中声明 `packageManager`，保证云端使用与本地一致的 pnpm 版本避免锁文件不兼容。
